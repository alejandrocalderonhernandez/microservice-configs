report:
  template: "{company} was founded in {foundation_date} by {founder} the websites from this company are {web_sites}"
fallback:
  uri: "http://localhost:8787/companies-crud-fallback/company/{name}"